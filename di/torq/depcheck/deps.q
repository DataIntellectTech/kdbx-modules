/ hard module dependencies and their minimum versions, validated by di.torq.depcheck
/ di.torq.depcheck has no hard dependencies - its only runtime dependency (log) is injected via init as a dictionary of
/ functions, following the same convention di.torq.depcheck itself checks other modules against
deps:(`$())!();
