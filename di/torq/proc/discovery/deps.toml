# Peer modules di.torq.proc.discovery relies on (see di/torq/depcheck/depcheck.md).
[dependencies]
"di.torq.servers" = "0.5.0"       # requires the INJECTED-servers contract (deps`servers) WITH getallservers+removeprocs, and .torq.servers.addprocs/removeprocs on peers
